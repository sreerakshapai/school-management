
<% include partials/header %>

<div class="container">
    <div class="row">
        <div class="col-md-3">
                 <h1><%= students.name %></h1>
                <p><%= students.batch %></p>
                <p><%= students.group %></p>
                <p><%= students.m1 %></p>
                <p><%= students.m2 %></p>
                <p><%= students.m3 %></p>
                <p><%= students.m4 %></p>
                <p><%= students.total %></p>
                <p><%= students.avg %></p>
                <p><%= students.result %></p>
                <p><%= students.grade %></p>
        </div>
    </div>
  
</div>

<% include partials/footer %>
